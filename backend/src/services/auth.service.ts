class AuthService {
    static async login(email:string, password:string, role:string){

    }

    static async register(email:string, password:string, role:string, username:string){

    }
}

export default AuthService;